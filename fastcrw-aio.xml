<?xml version="1.0"?>
<Container version="2">
  <Name>fastcrw-aio</Name>
  <Repository>dub19/fastcrw-aio:latest</Repository>
  <Registry>https://hub.docker.com/r/dub19/fastcrw-aio</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/wgross19/fastcrw-aio/issues</Support>
  <Project>https://github.com/wgross19/fastcrw-aio</Project>
  <Overview>fastCRW is a Firecrawl-compatible web scraping, crawling, and search API for AI agents on Unraid. Search, scrape, crawl, map, and extract in one container. Bundled JS renderer, native /mcp, zero credits, AGPL-3.0.

The REST API listens on port 3000 (default) with the Firecrawl-compatible /v1 surface: scrape, crawl, map, extract, search, and batch scrape. Point MCP-compatible agents (Claude Code, Cursor, and more) at http://[IP]:3000/mcp for 9 native tools (crw_search, crw_scrape, and friends). Full API docs are served at http://[IP]:3000/openapi.json.

Teach your agent when and why to use CRW with the official fastCRW agent skill (https://fastcrw.com/skills): a ~100-token SKILL.md that auto-triggers on web-data tasks, installed with one command (npx crw-mcp@latest install, supports Claude Code, Cursor, Gemini CLI, Codex, OpenCode, and Windsurf). Point the skill at this container by setting CRW_API_URL to http://[IP]:3000, then MCP handles execution.

First run:
- Scrape, Crawl, Map, and Extract work out of the box. Full JavaScript rendering is built in via the bundled LightPanda renderer.
- Set the SearXNG URL (CRW_SEARCH__SEARXNG_URL) to an external SearXNG instance with its JSON API enabled to activate /v1/search and research queries, e.g. http://192.168.1.100:8080 (your Unraid IP and SearXNG port).
- To use the heavy Chrome renderer tier, set Renderer Mode to chrome and supply an external CDP endpoint; Chrome is not bundled.
- Persistent configuration lives under /config (runtime crw config), and application data under /data.

Advanced settings (hidden by default): optional OpenAlex and Semantic Scholar API keys, API key gating for the REST API, and renderer memory limits.</Overview>
  <Changes>### 2026-08-24&#xD;
- Generated from CHANGELOG.md during release preparation. Do not edit manually.&#xD;
- Dockerfile builds crw-server v0.31.0 + LightPanda + s6&#xD;
- Replace template README with fastcrw product docs&#xD;
- S6 crw-server + lightpanda services and cont-init runtime config&#xD;
- Bundle AIO-tailored config.docker.toml matching /docker-compose/fastcrw&#xD;
- Author fastcrw-aio.xml defaults and advanced toggles&#xD;
- Register fastcrw-aio for aio-fleet (dub19 image, us/crw monitor)&#xD;
- Make fastcrw-aio trunk checks pass&#xD;
- Restore sources and CA bundle in runtime image&#xD;
- Use Docker Hub base namespace&#xD;
- Pin published aio-base digest&#xD;
- Bootstrap Unraid App Factory framework&#xD;
- Rename Unraid template to fastcrw-aio.xml&#xD;
- Allow full image build in integration gate&#xD;
- Use publish image tag in integration smoke&#xD;
- Enable fastcrw catalog sync</Changes>
  <Category>Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:3000]</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/wgross19/awesome-unraid/main/fastcrw-aio.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/wgross19/awesome-unraid/main/icons/fastcrw-aio.png</Icon>
  <ExtraSearchTerms>crawler scraper crw-server lightpanda searxng research scraping crawl map extract parse</ExtraSearchTerms>
  <Requires>An external SearXNG instance with the JSON API enabled is required for the search and research endpoints; scrape, crawl, map, extract, and parse work without it.</Requires>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText>Support wgross19 on GitHub Sponsors.</DonateText>
  <DonateLink>https://github.com/sponsors/wgross19</DonateLink>
  <Description/>
  <Networking>
    <Mode>bridge</Mode>
    <Publish>
      <Port>
        <HostPort>3000</HostPort>
        <ContainerPort>3000</ContainerPort>
        <Protocol>tcp</Protocol>
      </Port>
    </Publish>
  </Networking>
  <Data>
    <Volume>
      <HostDir>/mnt/user/appdata/fastcrw-aio/config</HostDir>
      <ContainerDir>/config</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
    <Volume>
      <HostDir>/mnt/user/appdata/fastcrw-aio/data</HostDir>
      <ContainerDir>/data</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
  </Data>
  <Environment/>

  <Config Name="API Port" Target="3000" Default="3000" Mode="tcp" Description="Host (LAN) port that exposes the fastCRW REST API, /mcp, and /openapi.json. The API always serves on container port 3000 (pinned in the image); change only this host-side port." Type="Port" Display="always" Required="true" Mask="false">3000</Config>
  <Config Name="SearXNG URL" Target="CRW_SEARCH__SEARXNG_URL" Default="" Mode="" Description="Required for Search and Research; Scrape, Crawl, Map, and Extract work without it. Point this at an external SearXNG instance with its JSON API enabled, e.g. http://192.168.1.100:8080 (your Unraid IP and SearXNG port)." Type="Variable" Display="always" Required="false" Mask="false"/>
  <Config Name="Renderer Mode" Target="CRW_RENDERER__MODE" Default="auto" Mode="" Description="Renderer to use: auto, lightpanda (bundled in-container), playwright, chrome (requires an external CDP endpoint; Chrome is not bundled), camoufox, or none." Type="Variable" Display="always" Required="false" Mask="false">auto</Config>

  <Config Name="Memory Limit" Target="CRW_MEM_LIMIT" Default="2g" Mode="" Description="Cap on crw-server renderer and document-sandbox memory (e.g. 2g). Raise it when running the heavy Chrome tier or larger parse pools." Type="Variable" Display="advanced" Required="false" Mask="false">2g</Config>

  <Config Name="OpenAlex API Key" Target="CRW_SEARCH__OPENALEX_API_KEY" Default="" Mode="" Description="Optional OpenAlex API key to lift rate limits on the research search engine. Unauthenticated access works without it." Type="Variable" Display="advanced" Required="false" Mask="true"/>
  <Config Name="OpenAlex Mailto" Target="CRW_SEARCH__OPENALEX_MAILTO" Default="" Mode="" Description="Optional contact email (mailto) sent to the OpenAlex polite pool. Helps reliability; unauthenticated access works without it." Type="Variable" Display="advanced" Required="false" Mask="false"/>
  <Config Name="Semantic Scholar API Key" Target="CRW_SEARCH__S2_API_KEY" Default="" Mode="" Description="Optional Semantic Scholar API key that boosts research result quality. Works without it." Type="Variable" Display="advanced" Required="false" Mask="true"/>
  <Config Name="API Keys" Target="CRW_AUTH__API_KEYS" Default="" Mode="" Description="Optional comma-separated API keys that gate the crw-server REST API. Leave empty for open access." Type="Variable" Display="advanced" Required="false" Mask="true"/>
</Container>
